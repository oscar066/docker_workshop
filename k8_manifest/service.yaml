apiVersion: v1
kind: Service
metadata:
  name: dev-webapp-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: dev-webapp
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
    nodePort: 30000
